# Data — 10.6 GiB, re-downloadable via urls-aria.txt
data/

# Derived caches, rebuilt by the notebooks
cache/

# Environment
.venv/
__pycache__/
*.py[cod]

# Jupyter
.ipynb_checkpoints/

# Editor / OS
.vscode/
.DS_Store

# LSTM checkpoints (resume state only; predictions + manifest are the deliverable)
experiments/results/**/lstm_checkpoint.pt
experiments/results/lstm_checkpoint.pt
# full hourly ladder parquet exceeds GitHub 100 MB; the _slim version is committed
experiments/results/lstm_hourly_q/lstm_hourly_test_predictions.parquet
